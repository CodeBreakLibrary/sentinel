// Copy this file to config.local.js (same folder) and fill in your real
// values. config.local.js is gitignored — never commit your real keys.

window.CESIUM_TOKEN = 'your-cesium-ion-access-token-here';

// Optional — only needed for the AIS vessel-tracking layer. Get a free key
// at https://aisstream.io. If this is left unset, that layer just stays
// off; nothing else in the app is affected.
window.AISSTREAM_API_KEY = '';
